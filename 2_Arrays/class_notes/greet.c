#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    if (argc ==2)
    {
    // Will say hello to the first line of your command
    printf("Hello, %s\n", argv[1]);
    }
    else
    printf("Hello.\n");
}